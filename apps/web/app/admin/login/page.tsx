'use client';

import { FormEvent, useState } from 'react';
import { useRouter } from 'next/navigation';
import { BROWSER_API_URL } from '@/lib/api';
import { setToken } from '@/lib/admin-api';

export default function LoginPage() { const router = useRouter(); const [error, setError] = useState(''); const [loading, setLoading] = useState(false); async function login(event: FormEvent<HTMLFormElement>) { event.preventDefault(); setLoading(true); setError(''); const form = new FormData(event.currentTarget); const response = await fetch(`${BROWSER_API_URL}/api/auth/login`, { method: 'POST', credentials: 'include', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ email: form.get('email'), password: form.get('password') }) }); setLoading(false); if (!response.ok) return setError('Correo o contraseña incorrectos.'); const data = await response.json(); setToken(data.accessToken); router.replace('/admin'); } return <main className="grid min-h-screen place-items-center bg-[#0c0c0d] px-6 text-[#f4f1ed]"><form onSubmit={login} className="w-full max-w-md border border-white/15 p-8 md:p-12"><p className="font-mono text-[10px] uppercase tracking-[.3em] text-[#8b78ff]">Portfolio CMS / Privado</p><h1 className="my-10 text-5xl font-bold tracking-tight">Acceso</h1><label className="mb-6 block font-mono text-[10px] uppercase tracking-widest">Correo<input name="email" type="email" required className="mt-2 w-full border-b border-white/20 bg-transparent py-3 text-base outline-none" /></label><label className="mb-8 block font-mono text-[10px] uppercase tracking-widest">Contraseña<input name="password" type="password" required minLength={8} className="mt-2 w-full border-b border-white/20 bg-transparent py-3 text-base outline-none" /></label>{error && <p className="mb-5 text-sm text-red-400">{error}</p>}<button disabled={loading} className="w-full bg-[#7057ff] py-4 font-mono text-[10px] uppercase tracking-widest">{loading ? 'Ingresando…' : 'Ingresar ↗'}</button></form></main>; }
