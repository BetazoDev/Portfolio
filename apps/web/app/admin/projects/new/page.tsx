'use client';
import Link from 'next/link';
import { FormEvent, useState } from 'react';
import { useRouter } from 'next/navigation';
export default function NewProject(){const router=useRouter();const [title,setTitle]=useState('');const [slug,setSlug]=useState('');const [summary,setSummary]=useState('');const [error,setError]=useState('');const submit=async(e:FormEvent)=>{e.preventDefault();const token=localStorage.getItem('portfolio_access_token');const r=await fetch(`${process.env.NEXT_PUBLIC_API_URL ?? 'http://localhost:4000'}/api/admin/projects`,{method:'POST',headers:{'Content-Type':'application/json',Authorization:`Bearer ${token}`},body:JSON.stringify({title,slug,shortSummary:summary,status:'draft'})});if(!r.ok){setError('Unable to save project. Check the slug and authentication.');return}router.push('/admin/projects')};return <main className="admin-shell"><aside className="admin-sidebar"><Link href="/admin" className="brand">HA<span>•</span></Link><nav><Link href="/admin/projects" className="side-active">Projects</Link></nav></aside><section className="admin-content narrow"><Link href="/admin/projects" className="back-link">← Projects</Link><span className="eyebrow">NEW CASE STUDY</span><h1>Create project</h1><form className="editor-form" onSubmit={submit}><label>Title<input value={title} onChange={e=>{setTitle(e.target.value);setSlug(e.target.value.toLowerCase().replace(/[^a-z0-9]+/g,'-').replace(/(^-|-$)/g,''))}} required /></label><label>Slug<input value={slug} onChange={e=>setSlug(e.target.value)} pattern="[a-z0-9]+(?:-[a-z0-9]+)*" required /></label><label>Short summary<textarea value={summary} onChange={e=>setSummary(e.target.value)} rows={5} /></label>{error&&<p className="form-error">{error}</p>}<button className="button button-primary">Save draft ↗</button></form></section></main>}
