'use client';
import { useEffect, useState } from 'react';
export function LanguageToggle() { const [language,setLanguage]=useState<'en'|'es'>('en'); useEffect(()=>{setLanguage(document.documentElement.dataset.language==='es'?'es':'en');},[]); function change(next:'en'|'es'){document.documentElement.dataset.language=next;document.documentElement.lang=next;localStorage.setItem('language',next);setLanguage(next);} return <div className="flex items-center gap-3 border-l border-[var(--border-color)] pl-5">{(['en','es'] as const).map(value=><button key={value} type="button" onClick={()=>change(value)} className={`font-mono text-[10px] uppercase tracking-widest transition ${language===value?'scale-110 font-bold text-[var(--accent)]':'opacity-40 hover:opacity-100'}`}>{value}</button>)}</div>; }
