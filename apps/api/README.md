# Portfolio API

Express REST API for the Portfolio CMS. Production startup applies Prisma migrations, runs the idempotent content seed, starts the HTTP service on port 4000 and verifies the `portfolio-media` Supabase Storage bucket.
